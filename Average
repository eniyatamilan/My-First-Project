#include <stdio.h>

int main() {
    int arr[5] = {1, 2, 3, 4, 5};
    int sum = 0;
    float avg;
    for (int i = 0; i < 5; i++) {
        sum += arr[i];
    }
    avg = (float)sum / 5;
    printf("Average of array elements: %.2f\n", avg);
    return 0;
}
